{
  "name": "publicsf.com",
  "version": "1.0.0",
  "description": "",
  "main": "publicsf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client && npm start",
    "build": "rm -r build && cd client && npm run build && cd ../"
  },
  "author": "Jonathan Crawford",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "multer": "^1.4.1",
    "npm": "^6.5.0"
  },
  "devDependencies": {}
}
